<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.blog.dao.users.usersDao2">

	<!-- 회원 가입 -->
	<insert id="register" parameterType="userdto">
		insert into
		users(uid,email,password) values(#{uid},#{email},#{password})
	</insert>

	<!-- 이메일 중복 체크 -->
	<select id="emailcheck" parameterType="String" resultType="userdto">
		select * from users where email=#{email} limit 1;
	</select>
	
	<!-- 회원정보 조회 -->
	<select id="search" parameterType="userdto" resultType="userdto">
		select * from users where uid=#{uid} and password=#{password} and email=#{email}
	</select>
	
	<!-- 회원정보 수정 -->
	<!-- <update id="modify" parameterType="userdto">
		update users set password=#{password}, uid=#{uid} where password=#{password} and email=#{email}
	</update> -->
	
	<!-- 회원정보 수정 -->
	<update id="modify" parameterType="userdto">
		update users set password=#{changePassword}, uid=#{changeUid} where password=#{password} and email=#{email}
	</update>
	
	<!-- 회원 탈퇴 -->
	<delete id="delete" parameterType="userdto">
		delete from users where password=#{password} and email=#{email}
	</delete>

	<!-- 로그인 -->
	<select id="login" parameterType="userdto" resultType="userdto">
		select email,uid from users where password=#{password} and email=#{email}
	</select>
</mapper>
